//scroll event listner

window.addEventListener('scroll', () => {
    const scrolled = window.scrollY;
    console.log(scrolled);
});


// main content fadeaway

// navbar 뿅 pop out from out of the windows

// navbar hovering event